# 🎯 Ứng dụng giám sát tài xế – Demo Python GUI với OpenCV + CustomTkinter + playsound

## 📦 Cấu trúc dự án

- `main.py`: Điểm khởi động app
- `modules/`: Chứa các phần code riêng biệt
- `assets/`: Lưu hình, âm thanh, icon
- `data/`: Ảnh chụp và file log

## ⚙️ Cách cài đặt

```bash
# 1. Tạo môi trường ảo
python -m venv venv

# 2. Kích hoạt (Windows)
venv\Scripts\activate

# 3. Cài thư viện
pip install -r requirements.txt

# 4. Chạy chương trình
python main.py
```
